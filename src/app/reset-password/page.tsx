import React from 'react'
import {ResetPassword} from '@/app-pages'
import {resetPassword} from '@/content'

const ResetPasswordPage: React.FC = () => {
  return <ResetPassword content={resetPassword} />
}

export default ResetPasswordPage
