/* eslint-disable */
import {signUp} from '@/content'
import {SignUp} from '@/app-pages'

const SignupPage = () => {
  const content = signUp

  return <SignUp content={content} />
}

export default SignupPage
